<%- include('../fragments/header') %>
<%
    const graphWidth = 150;
    const graphHeight = 20;
%>

            <content>
                <header>
                    <h1>Home</h1>
                    <h3>DrBrads Account</h3>
                </header>

                <fl-table>
                    <fl-head>
                        <row>
                            <cell>Domain</cell>
                            <cell>Status</cell>
                            <cell>Unique Visitors</cell>
                        </row>
                    </fl-head>
                    <fl-body>
<%
    const uniqueVisitors = [];
    for(const domain of data.domains){
        uniqueVisitors.push(domain.unique_visitors);
%>
                        <row>
                            <cell>
                                <a href='/domain/<%= domain.name %>'><%= domain.name %></a>
                            </cell>
                            <cell><%= domain.status ? 'Active' : 'Inactive' %></cell>
                            <cell>
                                <svg viewBox='0 0 <%= graphWidth %> <%= graphHeight %>'>
                                </svg>
                            </cell>
                        </row>
<%
    }
%>
                    </fl-body>
                </fl-table>
                <script>
                    const graphWidth = <%= graphWidth %>;
                    const graphHeight = <%= graphHeight %>;
                    const uniqueVisitors = <%- JSON.stringify(uniqueVisitors) %>;
                </script>
                <script src='/scripts/graph.js'></script>
                <script src='/scripts/home.js'></script>
            </content>

<%- include('../fragments/footer') %>
